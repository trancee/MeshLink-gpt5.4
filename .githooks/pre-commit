#!/usr/bin/env bash
set -euo pipefail

# Keep the local hook fast enough for regular commits while still catching
# formatting, static-analysis, and public-API drift before CI.
./gradlew :meshlink:ktfmtCheck :meshlink:detekt :meshlink:apiCheck
