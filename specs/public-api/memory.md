# Feature Memory

## Scope Notes
- Backfilled starter for existing feature `public-api`.
- Refresh this file against the current `spec.md`, `plan.md`, and `tasks.md` before further planning or implementation.

## Relevant Durable Memory
- Read `.specify/memory/constitution.md` first.
- Read `docs/memory/PROJECT_CONTEXT.md`, `ARCHITECTURE.md`, `DECISIONS.md`, and `BUGS.md` before resuming work.

## Open Questions
- Confirm whether `public-api` is still active, paused, or complete.
- Confirm whether the existing spec artifacts are still current.

## Watchlist
- Refresh `memory-synthesis.md` before changing plan, tasks, or implementation.
- Capture only durable, evidenced lessons back into `docs/memory/*`.
